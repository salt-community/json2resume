services:
  backend:
    extends:
      file: ./backend/docker-compose.yml
      service: backend

  frontend:
    extends:
      file: ./frontend/docker-compose.yml
      service: frontend
    depends_on:
      - backend
